filebeat.modules:
  - module: nginx
    access:
      enabled: true
      var.paths: ["/logs/access.log*"]
    error:
      enabled: true
      var.paths: ["/logs/error.log*"]

output:
  logstash:
    enabled: true
    hosts:
      - logstash:5044